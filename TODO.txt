Important:
- Add an absolute LM solver (Done, but it's extremely slow, probably because of line search. Program lm solver myself, without line search!)
- Add mesh convergence utility
- Add anomaly insertion (in the mesh, not just editing the elements) utility
- Make a tutorial for different solvers.

Later:
- Higher order forward solver
- Sensor position optimization utility

Maybe:
- Interface with EIDORS inv_solve() by detecting that the model is mdeit automatically. At least, do an inv_solve_mdeit that handles multiple solvers.